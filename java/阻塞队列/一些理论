多线程领域，所谓阻塞，在某些情况下会挂起线程即阻塞，在某些情况下又会被唤醒

需要blockQueue，不需要感知底层什么时候需要阻塞队列，什么时候需要唤醒线程，这一些blockqueue给你报办了

在concurrent包发布以前，程序员需要自己手动控制细节，还要兼顾效率和线程安全，而这会给我们带来不小的复杂度



SynchronousQueue 与其他BlockQueue不同，SynchronousQueue是一个不存储元素的BlockQueue
每个put操作要等待一个taken操作，否则不能继续添加元素